# Halo's bundler loads the Vue compiler and its AST helpers from CommonJS
# compatibility paths. A hoisted layout keeps those peer dependencies visible
# to rspack-vue-loader when Gradle invokes pnpm from the project root.
node-linker=hoisted
# Gradle and Halo DevTools invoke pnpm without an interactive terminal.
confirmModulesPurge=false
